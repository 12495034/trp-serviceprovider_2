//increment in minutes when adding a new appointment slot
export const appointInc = 30;

//icon size on clinic detail screen
export const iconSize = '20';

//locale format of time string, "en-GB" specified as "en-US" is the default
export const timeZones = ["en-GB"];

//Set the max clinic capacity
export const maxClinicCapacity = 19

//set progress spinner type
export const spinnerType = "border"
export const spinnerColor = "info"


